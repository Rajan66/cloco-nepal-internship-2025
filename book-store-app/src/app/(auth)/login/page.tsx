import LoginForm from "@/features/auth/components/LoginForm";
import React from "react";

// this is '/' route, change the route after adding homepage
const page = () => {
  return (
    <div className="">
      <LoginForm />
    </div>
  );
};

export default page;
